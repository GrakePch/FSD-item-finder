import { createContext } from "react";

export const AllTerminalsContext = createContext([]);
export const AllItemsPriceContext = createContext([]);
export const BodiesAndLocationsContext = createContext([{}, {}, {}]);