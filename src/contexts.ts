import { createContext } from "react";

// Replace [] and [{}] with appropriate types if you have them
export const AllTerminalsContext = createContext<any>({});
export const AllItemsPriceContext = createContext<any>({});
export const BodiesAndLocationsContext = createContext<any[]>([{}, {}, {}]);